---
const { title, coverImage, description } = Astro.props;
---

<header class="header">
  <div>
    <img src={coverImage} alt={title} />
    <h1>{title}</h1>
    <p>{description}</p>
  </div>
</header>
